[
  {
    "id": "comc-trunk-ceo",
    "description": "CEO trunk: login as qa-test-journeyhawk@phronex.com (cc_ceo role, ComC premium grant). Saves browser state for all per-org leaf journeys.",
    "isSharedRoot": true,
    "depth": 0,
    "steps": [
      {
        "id": 1,
        "description": "CEO TRUNK FRESH LOGIN \u2014 DO NOT load any browser state file. This journey CREATES the session. Start fresh: use browser_tabs to close all tabs except the current one. Use browser_evaluate to wipe the service worker cache: 'await caches.keys().then(ks => Promise.all(ks.map(k => caches.delete(k))))'. Then navigate directly to http://localhost:3002/auth/login?t=Date.now() (cache-bust). VERIFY the login form is visible (email+password fields must be present in the snapshot). Fill email 'qa-test-journeyhawk@phronex.com' and password 'HMqt4kH5loXkeAtvQSndQ7zNmow!Phx#9'. Submit and wait for the portal sidebar to appear. Mark failed if redirected to a non-login page before credentials are submitted."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre. Verify the Command Centre section loads without a 500 error. The page should show any Command Centre content \u2014 a dashboard, navigation links, or department views. If the page redirects to login or shows 403/404, mark failed."
      },
      {
        "id": 3,
        "description": "CRITICAL \u2014 Save browser session state with cookies. Call the mcp__cctr-playwright__browser_storage_state tool with filename \"/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-ceo-state.json\". After calling the tool, VERIFY the file was correctly saved by using browser_run_code to execute: const fs = require('fs'); const d = JSON.parse(fs.readFileSync('/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-ceo-state.json', 'utf8')); console.log('cookies:', d.cookies ? d.cookies.length : 0); If the cookies count is 0 (empty state), the primary tool failed \u2014 use browser_run_code as the FALLBACK to save actual storage state: const fs = require('fs'); const state = await page.context().storageState(); fs.writeFileSync('/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-ceo-state.json', JSON.stringify(state)); console.log('saved cookies:', state.cookies.length); Mark this step FAILED if the final file still has 0 cookies \u2014 an empty state file will cause all 3 dependent CEO leaf journeys to fail."
      }
    ]
  },
  {
    "id": "comc-trunk-manager",
    "description": "Trunk: establish authenticated session for cc_manager role QA account (qa-test-comc-manager@phronex.com). Required by journeys that test manager-level RBAC boundaries.",
    "depth": 0,
    "isSharedRoot": true,
    "steps": [
      {
        "id": 1,
        "description": "MANAGER TRUNK FRESH LOGIN \u2014 DO NOT load any browser state file. This journey CREATES the session. Start fresh: use browser_tabs to close all tabs except the current one. Use browser_evaluate to wipe the service worker cache: 'await caches.keys().then(ks => Promise.all(ks.map(k => caches.delete(k))))'. Then navigate directly to http://localhost:3002/auth/login?t=Date.now() (cache-bust). VERIFY the login form is visible (email+password fields must be present in the snapshot). Fill email 'qa-test-comc-manager@phronex.com' and password 'v492kb-JXn5Sso1TEKApd2wi9LE'. Submit and wait for the portal sidebar to appear or redirect to http://localhost:3002/command-centre/dashboard. Mark failed if redirected back to http://localhost:3002/auth/login (wrong credentials) or if a 500 error appears."
      },
      {
        "id": 2,
        "description": "Verify this session has manager-level access, not CEO-only access. Use browser_network_request to GET http://localhost:3002/api/admin/command-centre/api/v1/meetings/upcoming \u2014 expect 200 with a JSON array (meetings endpoint uses require_manager, so cc_manager can access it). Then use browser_network_request to GET http://localhost:3002/api/admin/command-centre/api/v1/agents \u2014 expect 403 with detail 'CEO role required' (agents endpoint uses require_ceo, which cc_manager does NOT satisfy). PASS if meetings/upcoming=200 AND agents=403. FAIL if agents returns 200 (account has CEO-level access, not manager-level) or if meetings/upcoming returns 403 (cc_manager role was not correctly granted)."
      },
      {
        "id": 3,
        "description": "CRITICAL \u2014 Save browser session state with cookies. Call the mcp__cctr-playwright__browser_storage_state tool with filename \"/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-manager-state.json\". After calling the tool, VERIFY the file was correctly saved by using browser_run_code to execute: const fs = require('fs'); const d = JSON.parse(fs.readFileSync('/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-manager-state.json', 'utf8')); console.log('cookies:', d.cookies ? d.cookies.length : 0); If the cookies count is 0 (empty state), the primary tool failed \u2014 use browser_run_code as the FALLBACK to save actual storage state: const fs = require('fs'); const state = await page.context().storageState(); fs.writeFileSync('/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-manager-state.json', JSON.stringify(state)); console.log('saved cookies:', state.cookies.length); Mark this step FAILED if the final file still has 0 cookies \u2014 an empty state file will cause all dependent manager-role leaf journeys to fail."
      }
    ]
  },
  {
    "id": "comc-trunk-superadmin",
    "description": "Trunk: portal login as superadmin with ComC grant, verify dashboard loads with 7 pillar nav, save browser state.",
    "isSharedRoot": true,
    "stateOutputPath": ".tmp/comc-trunk-superadmin-state.json",
    "depth": 0,
    "steps": [
      {
        "id": 1,
        "description": "TRUNK FRESH LOGIN \u2014 DO NOT load any browser state file. This journey CREATES the session. Do NOT call browser_set_storage_state. Start fresh: use browser_tabs to close all tabs except the current one. Use browser_evaluate to wipe the service worker cache: 'await caches.keys().then(ks => Promise.all(ks.map(k => caches.delete(k))))'. Then navigate directly to http://localhost:3002/auth/login?t=Date.now() (cache-bust). VERIFY the login form is visible (email+password fields must be present in the snapshot). Fill email 'qa-test-journeyhawk@phronex.com' and password 'HMqt4kH5loXkeAtvQSndQ7zNmow!Phx#9'. Submit and wait for the portal sidebar to appear. Mark failed if redirected to a non-login page before credentials are submitted."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/dashboard. Verify the ComC dashboard section loads without a 500 error or blank white screen. The dashboard main area should show stat cards (Org Health, Active Agents, Pending Approvals). Verify NO JavaScript TypeError appears in the browser console \u2014 if 'toFixed' or 'Cannot read properties of undefined' appears in console errors, this is a stale browser cache serving the old JS bundle: record the chunk filename from the error and mark the step as failed. Verify the left sidebar shows the Command Centre pillar sections (Pulse, Revenue, People, Operations, Strategy, Intelligence, Config). If the page redirects elsewhere or shows 403/404, flag immediately."
      },
      {
        "id": 3,
        "description": "IMPORTANT \u2014 Save browser session: Call the mcp__cctr-playwright__browser_storage_state tool with filename set to \"/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json\" to save cookies and localStorage for downstream test nodes. This step is critical \u2014 without it, dependent journeys will fail."
      }
    ]
  },
  {
    "id": "comc-agents-idparam-portrait",
    "description": "[pulse] Agents Idparam Portrait \u2014 Full DEEP journey covering CRUD, validation, and persistence + cross-service integration",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json'. If session expired, navigate to http://localhost:3002/command-centre/login and re-authenticate using the superadmin credentials (email: superadmin@phronex.local, password: <stored>). Verify JWT token is present in Authorization header (Bearer <token>) and contains org_id claim."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/agents and verify the Agent List page loads (HTTP 200). Locate and click on an existing agent row with status='active' (e.g., 'Finance Agent' in Finance department). Verify the browser navigates to http://localhost:3002/command-centre/agents/[agent_uuid]/portrait and the page renders without console errors, blank states, or 'Something went wrong' error messages."
      },
      {
        "id": 3,
        "description": "On the Agent Portrait page, verify the following fields are displayed and populated: agent name (text), department (dropdown, e.g. 'Finance'), role (text, e.g. 'Risk Analysis Specialist'), status badge (colour-coded, green=active), capabilities (jsonb array rendered as tag list), model_id (text), and updated_at timestamp. Confirm all fields match the GET /api/v1/agents/{agent_id} HTTP 200 response."
      },
      {
        "id": 4,
        "description": "Test VALIDATION: In the Agent Portrait form, clear the 'name' field and attempt to save (click 'Save' button or trigger form submission). Verify inline error message appears near the name field ('Name is required' or equivalent). Confirm HTTP 400 Bad Request is logged in network tab. Restore the name field with a valid value (e.g., 'Finance Agent Updated') and verify the error clears."
      },
      {
        "id": 5,
        "description": "Test PERSISTENCE: With a valid agent name in the form, click the 'Save' button. Verify HTTP PATCH /api/v1/agents/{agent_id} returns 200 OK with updated_at timestamp. Close the portrait page by navigating to http://localhost:3002/command-centre/dashboard (click Dashboard in left sidebar). Verify the dashboard loads and displays the agent in the agent status widget. Navigate back to http://localhost:3002/command-centre/agents/[same_agent_uuid]/portrait and confirm the updated name persists in the form field and matches the API response."
      },
      {
        "id": 6,
        "description": "Test CROSS-SERVICE INTEGRATION: On the Agent Portrait page, change the agent's status from 'active' to 'paused_ceo' via a dropdown or toggle control. Click 'Save'. Verify HTTP PATCH /api/v1/agents/{agent_id}/pause returns 200 OK and agent.status field updates to 'paused_ceo'. Navigate to http://localhost:3002/command-centre/agents (Agent List) and confirm the agent row now displays status badge as 'paused' (yellow/grey colour). Verify the Paperclip backend state is consistent by checking the agent_status widget on the dashboard reflects the paused state."
      },
      {
        "id": 7,
        "description": "Test IDEMPOTENCY: While the agent remains paused, navigate back to http://localhost:3002/command-centre/agents/[agent_uuid]/portrait. Click 'Save' without making changes. Verify HTTP PATCH is sent but returns 200 OK (no idempotency error). Resume the agent by changing status back to 'active' and save. Verify HTTP 200 OK and status badge immediately reflects 'active' (green) both on the portrait page and when navigating to http://localhost:3002/command-centre/agents list."
      },
      {
        "id": 8,
        "description": "Test SESSION PERSISTENCE: With the agent portrait page open, refresh the browser (Ctrl+R or F5). Verify the page reloads with the same agent_id in the URL, JWT token remains valid, and all agent fields are re-fetched and displayed correctly (no login redirect, no 401 errors). Confirm network tab shows successful GET /api/v1/agents/{agent_id} call after refresh. Close the browser tab, reopen a new tab, and navigate to http://localhost:3002/command-centre/agents/[same_agent_uuid]/portrait. Verify the session cookie/JWT allows instant page load without re-authentication."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-agents-learning",
    "description": "[pulse] Agents Learning \u2014 Full DEEP journey covering CRUD, validation, and persistence + cross-service integration",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json' or navigate to http://localhost:3002/command-centre/dashboard and log in if session expired. Verify JWT token is valid and contains org_id claim. Confirm superadmin role by checking presence of Admin pillar in left sidebar."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/agents/learning. Verify page loads without errors: no blank page, no 'Something went wrong' error modal, no console errors. Check that the page title displays 'Agents Learning' and the breadcrumb shows Pulse > Agents > Learning."
      },
      {
        "id": 3,
        "description": "Verify the agents learning data table renders correctly. Confirm either: (a) at least one row appears with columns for agent_id, agent_name, learning_topic, competency_level, and last_updated, OR (b) an explicit empty state message is shown ('No learning records yet' or similar). Verify no blank sections or truncated columns exist."
      },
      {
        "id": 4,
        "description": "Locate the 'Add Learning Record' form or button. Fill in required fields: select an agent from 'Agent' dropdown (pick first active agent from agents list), enter 'Learning Topic' text field with value 'API Integration Strategy', set 'Competency Level' to 'Intermediate' via radio button or select, optionally add 'Notes' text area with 'Testing cross-service flow'. Click 'Submit' button. Verify HTTP 201 response from POST /api/v1/agents/{agent_id}/learning with new learning_record object containing id, org_id, agent_id, topic, competency_level, and created_at timestamp. Confirm toast notification appears ('Learning record created successfully') and form clears or redirects."
      },
      {
        "id": 5,
        "description": "Test validation: attempt to submit the form with empty 'Learning Topic' field. Verify HTTP 400 Bad Request response or inline validation error appears below the field with message 'Learning Topic is required'. Confirm form does not submit and no toast notification fires. Fill in the topic field and verify error clears before resubmit."
      },
      {
        "id": 6,
        "description": "Verify API contract boundary: confirm the newly created learning record now appears in the agents learning data table in real-time (no page refresh required, or auto-refresh within 2 seconds). Validate the row displays the correct agent name (matched via agent_id FK), topic, competency level, and a timestamp matching the created_at from the API response. This confirms the POST response payload is correctly reflected in the GET /api/v1/agents/learning list view."
      },
      {
        "id": 7,
        "description": "Test persistence: navigate away from http://localhost:3002/command-centre/agents/learning to http://localhost:3002/command-centre/dashboard (click 'Pulse' pillar or 'Dashboard' link). Verify dashboard loads and displays metric cards. Then navigate back to http://localhost:3002/command-centre/agents/learning. Confirm the learning record created in Step 4 is still visible in the table with the same values (agent_name, topic, competency_level, timestamp). Verify GET /api/v1/agents/learning returns HTTP 200 and includes the persisted record from the database."
      },
      {
        "id": 8,
        "description": "Edit the learning record: click 'Edit' button (or pencil icon) on the row created in Step 4. Change 'Competency Level' from 'Intermediate' to 'Advanced'. Click 'Save'. Verify HTTP 200 response from PATCH /api/v1/agents/{agent_id}/learning/{learning_id} with updated_at timestamp. Confirm the table row updates in-place to show 'Advanced' without requiring a page reload. Navigate away and back to verify the updated value persists in the database."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-biz-error-states-recovery",
    "description": "[BEHAVIORAL] Error states and recovery paths: validate error banners, retry logic, and graceful degradation across key ComC pages",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json'. If session expired (no valid JWT in localStorage), navigate to http://localhost:3002/command-centre/login, enter superadmin credentials (email: superadmin@phronex.internal, password from ENV), and verify redirect to http://localhost:3002/command-centre/dashboard with JWT present in Authorization header."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/agents. Verify page loads (HTTP 200) and agent list table renders with columns: Name, Department, Role, Status, Updated At. Seed test data: ensure at least 1 agent exists with status='active'. If list is empty, POST /api/v1/agents with body {name: 'Test Agent', role: 'finance', department_id: <valid_dept_id>, org_id: <superadmin_org_id>} and verify HTTP 201 response; agent appears in table within 2 seconds."
      },
      {
        "id": 3,
        "description": "Test ERROR DISPLAY state: Navigate to a non-existent agent detail page (e.g., http://localhost:3002/command-centre/agents/nonexistent-uuid-1234). Verify the page shows a graceful error message (e.g., 'Agent not found', 404 page, or redirect back to agents list) \u2014 NOT a blank screen, JavaScript crash, or raw stack trace. PASS if error is handled gracefully."
      },
      {
        "id": 4,
        "description": "Test CRUD + cross-service integration: Click 'Pause' action on an active agent row. Verify PATCH /api/v1/agents/{agent_id}/pause returns HTTP 200 with updated agent object (status='paused_ceo', updated_at refreshed). Verify agent row status badge changes to yellow immediately (optimistic update) or within 500ms (server-driven). Verify agent's paused state is reflected in downstream /api/v1/dashboard metrics (agent count under 'Active Agents' decreases by 1) by navigating to http://localhost:3002/command-centre/dashboard and observing metric card updates."
      },
      {
        "id": 5,
        "description": "Test RECOVERY path: Resume the paused agent by clicking 'Resume' action. Verify PATCH /api/v1/agents/{agent_id}/resume returns HTTP 200 with status='active'. Verify agent row updates (badge returns to green). Verify http://localhost:3002/command-centre/dashboard metric cards update within 1 second (Active Agents count increases by 1). Verify idempotent behavior: click Resume again immediately; verify server returns HTTP 200 with no state change (agent already active) and no duplicate updates in UI."
      },
      {
        "id": 6,
        "description": "Test PERSISTENCE across navigation: From http://localhost:3002/command-centre/agents, note the current state of the test agent (status, name, updated_at timestamp). Navigate to http://localhost:3002/command-centre/dashboard (different page). Wait 2 seconds, then navigate back to http://localhost:3002/command-centre/agents. Verify agent row retains the same state (status unchanged, name unchanged, updated_at timestamp reflects server state, not stale cache). Verify no 'Unsaved Changes' warning appears and no data loss has occurred."
      },
      {
        "id": 7,
        "description": "Test ERROR RECOVERY with network failure simulation: In http://localhost:3002/command-centre/agents, attempt to pause an agent while simulating network timeout (DevTools Network > throttling, or mock API delay >30s). Verify UI shows loading spinner or disabled state on the action button. After timeout, verify error toast appears with message 'Failed to pause agent. Please try again.' Verify agent row reverts to previous state (status remains 'active'). Click 'Retry' on error toast; verify PATCH request is sent again and succeeds (HTTP 200), agent updates, and toast dismisses."
      },
      {
        "id": 8,
        "description": "Test TENANT_ISOLATION error handling: Verify superadmin JWT contains correct org_id claim (decode JWT in DevTools Console: console.log(jwt_decode(localStorage.getItem('jwt')))). Attempt to access /api/v1/agents?org_id=<different_org_id> (via network panel or direct fetch). Verify API returns HTTP 403 Forbidden with message 'Access denied: organisation mismatch'. Verify UI does NOT display agents from other organisations. Verify no cross-organisation data leakage occurs in browser storage or DOM."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-biz-meeting-prep-llm-generation-and-caching",
    "description": "[BEHAVIORAL] Verify Meeting Prep auto-generates LLM content on first call, caches it on subsequent calls without regeneration, and persists notes/action items. Tests data integrity across LLM timeout/empty prep failures, cache staleness, and note persistence.",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json' using the browser storage state tool. Verify the session is active by checking no login prompt appears."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/dashboard. Click on 'Meetings' in the left navigation menu to access the meetings list view. Wait for the page to load and verify at least one meeting is visible in the list. Record the first meeting ID from the URL or meeting card (e.g., {{meeting_id}})."
      },
      {
        "id": 3,
        "description": "Click on a meeting card to open the meeting detail page (URL will be http://localhost:3002/command-centre/meetings/{{meeting_id}}). Look for a 'Prep' button, 'Generate Prep', or 'View Prep' action on the page. Click it to trigger the first prep content fetch."
      },
      {
        "id": 4,
        "description": "Wait for the prep content to load and display (LLM generation may take up to 10 seconds). Once loaded, verify that prep_content contains: (a) auto-generated context about the meeting topic, (b) at least 2-3 talking points, (c) attendee background information if attendees are listed. Record the exact prep content text and a timestamp {{prep_fetch_1_time}} of when it loaded. If an error appears (LLM_TIMEOUT, EMPTY_PREP), note it and proceed to failure branch; otherwise continue to step 5."
      },
      {
        "id": 5,
        "description": "Without navigating away from the meeting detail page, refresh the browser (Ctrl+R or Cmd+R). Wait for the page to reload and the prep section to re-render. Click the 'Prep' button or view prep again. Record the timestamp {{prep_fetch_2_time}} and the new prep content text. Verify that: (a) the prep content is identical to step 4 (same talking points, same context, same attendee background), (b) the fetch completed faster than step 4 (indicating cache hit, not LLM regeneration), (c) no 'Generating...' spinner or loading state appears. If content differs or regeneration occurs, this is a REGEN_ON_EVERY_CALL failure."
      },
      {
        "id": 6,
        "description": "On the same meeting detail page, look for a 'Notes' section or 'Add Notes' button. Click it to open a notes editor. Type a test note string: 'Follow up on Q3 budget approval with Finance team'. Below the notes field, find an 'Action Items' section and click 'Add Action Item'. Add 3 action items: (1) 'Schedule Finance discussion', (2) 'Prepare budget slides', (3) 'Confirm attendee availability'. Record these exact strings {{notes_text}} and {{action_items_array}}."
      },
      {
        "id": 7,
        "description": "Click 'Save' or 'Submit' on the notes form. Verify a success message appears (e.g., 'Notes saved') and the form closes. Wait 2 seconds for any async persist operations to complete. Record the save timestamp {{notes_save_time}}."
      },
      {
        "id": 8,
        "description": "Navigate away from the meeting detail page by clicking 'Meetings' in the left menu to return to the meetings list. Wait 3 seconds, then click back into the same meeting to re-open its detail page (URL: http://localhost:3002/command-centre/meetings/{{meeting_id}}). Scroll to the Notes section. Verify that: (a) the notes text exactly matches {{notes_text}}, (b) all 3 action items are present with identical text, (c) the section displays 'Last saved: [timestamp near {{notes_save_time}}]'. If notes or action items are missing or truncated, this is a NOTES_LOST or ACTION_ITEMS_IGNORED failure. Verify the prep content is still cached and unchanged from step 5 (STALE_CACHE check)."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-biz-meetings-list-empty-state",
    "description": "[BEHAVIORAL] Verify that the Meetings List correctly handles empty state (no upcoming meetings) and returns HTTP 200 with an empty array without crashing or showing error states. Tests resilience and proper empty state UI.",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json' using the browser storage state tool. Verify the session is active by checking no login prompt appears."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/meetings. If the meetings list shows upcoming meetings, this test will verify the page loads correctly with data. If the list is empty, verify an appropriate empty-state message is displayed (e.g., 'No upcoming meetings', empty calendar icon, or placeholder text). PASS if either meetings are listed OR empty-state is displayed \u2014 FAIL only if page shows a spinner indefinitely, blank panel, or error."
      },
      {
        "id": 3,
        "description": "Navigate to http://localhost:3002/command-centre/meetings. The page loads and sends GET /api/v1/meetings/upcoming. Verify HTTP 200 is returned. Verify the response body is an empty array: [] (not null, not undefined, not an error object). Record this as {{empty_response_verified}}."
      },
      {
        "id": 4,
        "description": "Verify the left panel displays a 'No upcoming meetings' or 'No meetings scheduled' placeholder message (or similar empty state text). Verify the right panel is either hidden, shows a placeholder, or remains blank. Verify no error message appears, no spinner is stuck in a loading state, and no JavaScript console errors are logged. If an error message or stack trace is visible, record as failure mode: WRONG_STATUS or CRASH."
      },
      {
        "id": 5,
        "description": "Verify the page remains functional: navigation elements (menu, logo, back button) are clickable and responsive. The layout does not break; the two-panel structure is still visibly present even in the empty state. If the layout collapses or becomes unusable, record as failure mode: LAYOUT_BROKEN."
      },
      {
        "id": 6,
        "description": "Navigate away (to another Command Centre page) and return to http://localhost:3002/command-centre/meetings. Verify the empty state is re-rendered consistently, and another GET /api/v1/meetings/upcoming call is made and returns HTTP 200 with an empty array. The behavior must be repeatable and idempotent."
      },
      {
        "id": 7,
        "description": "Restore test data (re-populate meetings or return to the original test environment) so subsequent journeys have meetings to work with. Verify the list is now populated again by navigating to http://localhost:3002/command-centre/meetings and confirming meetings are visible in the left panel."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-costs",
    "description": "[operations] Costs \u2014 Full DEEP journey covering CRUD, validation, and persistence + cross-service integration",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json' or navigate to http://localhost:3002/command-centre/costs and log in if session expired. Verify JWT token is present in localStorage and not expired (check 'exp' claim)."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/costs. Verify page loads without errors (no blank page, no 'Something went wrong' message, no 500/4xx in Network tab). Confirm main heading 'Costs' and sidebar 'Operations' pillar are visible. Assert no console errors (severity \u2265 error)."
      },
      {
        "id": 3,
        "description": "Verify the Costs Overview data table renders with 4-level rollup: Org Total row, expandable Department rows (showing dept_type, hard_limit_usd, soft_limit_minutes), Agent rows nested under departments, and Per-Call Cost columns. If no cost data exists, confirm empty state message 'No cost data for selected period' is shown (not a blank section). Open Network tab and verify GET /api/v1/costs/summary returns HTTP 200 with cost structure matching schema."
      },
      {
        "id": 4,
        "description": "Verify the Costs page displays data in a multi-tab layout (Overview, By Department, By Agent, By Task Type, Efficiency). Click each tab and verify content loads without blank sections. The Overview tab should show a cost summary. By Department tab should show per-department breakdown. PASS if all tabs load with data or appropriate empty-state messages."
      },
      {
        "id": 5,
        "description": "Test persistence: Navigate away from the Costs page to http://localhost:3002/command-centre/dashboard. Wait 2 seconds. Navigate back to http://localhost:3002/command-centre/costs. Verify the same tab layout and data renders consistently. PASS if data persists across navigation."
      },
      {
        "id": 6,
        "description": "Verify cross-service API contract: Cost Efficiency integration. Locate the 'Efficiency Recommendations' card or tab. Confirm GET /api/v1/costs/efficiency is called (verify in Network tab, HTTP 200). If sufficient cost history exists (\u22657 days), verify response contains per-dept analysis, over-budget alerts, and optimization suggestions. Confirm each suggestion includes an LLM-generated message (via phronex-common LLM provider). If insufficient data (<7 days), verify response includes 'Insufficient data' message with recommendation to check back after 7 days. Assert no LLM_TIMEOUT (request completes within 8 seconds)."
      },
      {
        "id": 7,
        "description": "Verify budget data is accessible: Using page.evaluate(), send a GET request to http://localhost:3002/api/admin/command-centre/api/v1/costs/budget. Verify HTTP 200 response with a JSON array of budget allocation rows. Each row should have at minimum: dept_id or agent_id, period, and a limit field. PASS if the API returns valid budget data (array may be empty \u2014 that is acceptable)."
      },
      {
        "id": 8,
        "description": "Verify audit trail integration: Open browser DevTools, go to Network tab, filter by 'audit' or 'log'. Perform an action in the Costs page (e.g., edit budget). Confirm that monitoring.audit.AdminAuditLog middleware logs the action with actor_id (JWT sub: '061bfa0'), action type ('cost_budget_updated'), and timestamp. Cross-check the audit entry by navigating to http://localhost:3002/command-centre/admin/audit-logs (if accessible). Verify the cost action appears in the audit log with correct actor, action, and timestamp matching the UI action."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-costs-efficiency",
    "description": "[operations] Costs Efficiency \u2014 Full DEEP journey covering CRUD, validation, and persistence + cross-service integration",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json' or navigate and log in if session expired. Verify JWT token is present in localStorage and valid (check auth.dependencies validation passes)."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/costs/efficiency. Verify the page loads without errors: no blank page, no 'Something went wrong' error banner, browser console free of 500/400 errors, and response from GET /api/v1/costs/efficiency returns HTTP 200."
      },
      {
        "id": 3,
        "description": "Verify the Costs Efficiency data section renders correctly. Check for: (a) a table/card showing per-department efficiency analysis with columns: Department, Total Cost (USD), Efficiency Score (%), Status (Over-budget/On-track); (b) if cost history <7 days, confirm 'insufficient data' message appears instead of recommendations; (c) if sufficient history, verify at least one department row with realistic efficiency metrics (0\u2013100%). Confirm no ROLLUP_MISMATCH: spot-check that sum of visible department costs aligns with org-level total shown in summary widget."
      },
      {
        "id": 4,
        "description": "Cross-service validation: Click on a department row (e.g., 'Engineering') to expand or drill-down. Verify that the UI triggers GET /api/v1/costs/efficiency?department=Engineering (inspect Network tab) and receives HTTP 200 with per-agent cost breakdowns. Confirm the response structure includes agent_id, call_count, total_cost_usd, and avg_cost_per_call fields. Verify the expanded view renders these agent-level metrics in a nested table or collapsible section."
      },
      {
        "id": 5,
        "description": "Validation step \u2014 attempt to enter an invalid budget threshold. Locate the 'Set Department Budget Limit' form (if present) or open an edit modal for a department's soft_limit_minutes. Enter a negative value (e.g., -100) and attempt to submit. Verify that the UI displays an inline error message (e.g., 'Budget limit must be a positive number') and the request is NOT sent (confirm no outbound POST /api/v1/costs/budget in Network tab). Confirm the form state reverts to the previous valid value."
      },
      {
        "id": 6,
        "description": "Persistence step \u2014 from the Costs Efficiency page, navigate to a different pillar (e.g., http://localhost:3002/command-centre/pulse/dashboard). Wait 2 seconds, then navigate back to http://localhost:3002/command-centre/costs/efficiency. Verify that: (a) the page reloads and GET /api/v1/costs/efficiency is called again (HTTP 200); (b) the same department rows and efficiency scores are displayed; (c) any previously expanded department sections are collapsed (no client-side state pollution); (d) no orphaned data or duplicate rows appear. Confirm audit log captures the navigation event (via monitoring.audit AdminAuditLog with actor_id from JWT sub)."
      },
      {
        "id": 7,
        "description": "LLM integration boundary test \u2014 verify that efficiency recommendations (if displayed) were generated by the LLM provider. Check that the page renders an 'AI-Generated Insights' section with optimization suggestions (e.g., 'Reduce token usage in Agent X by switching to GPT-3.5 Turbo for summarization'). Confirm the insights panel includes a timestamp and a disclaimer badge. Verify that if LLM generation times out (>30s), the UI falls back gracefully with a message 'Recommendations generating\u2014refresh shortly' instead of hanging or throwing an error."
      },
      {
        "id": 8,
        "description": "Final state validation \u2014 confirm that all cost data visible on the page matches the backend API contract. Export or copy the efficiency table data and cross-check a sample row: pick one department, verify its total_cost_usd + soft_limit_usd values match a fresh call to GET /api/v1/costs/budget?department=X (HTTP 200). Verify that the efficiency score formula is correctly applied: e.g., Efficiency = (Budget Remaining / Budget Allocated) * 100 for on-budget departments. Confirm no console warnings or memory leaks are present (check DevTools Performance/Memory tabs after 30-second idle)."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-departments-deptidparam",
    "description": "[general] Departments Deptidparam \u2014 Full DEEP journey covering CRUD, validation, and persistence + cross-service integration",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json'. Verify JWT token is valid and contains org_id claim. If session expired (> 1 hour old), navigate to http://localhost:3002/command-centre/login, enter superadmin credentials (email: superadmin@phronex.local, password from .env), and click 'Sign In'. Verify redirect to http://localhost:3002/command-centre/dashboard and JWT is saved to session file."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/departments. Verify page loads without errors (no blank page, no 'Something went wrong', no JavaScript console errors). Confirm 'Departments' header is visible and a table or card list of departments renders. Verify table columns include: Department Name, Agents Count, Status (Active/Inactive), Last Heartbeat, Isolation Mode. HTTP GET /api/v1/departments should return 200 with array of departments filtered by caller's org_id."
      },
      {
        "id": 3,
        "description": "From the departments list, identify a department with at least 1 assigned agent. Click on the department row to navigate to http://localhost:3002/command-centre/departments/[deptId] (e.g., /departments/d-12345). Verify the detail page loads without errors. Confirm the following elements render: Department Name input field (pre-filled with current name), Active/Inactive toggle switch, Agent List section showing assigned agents with name, role, and status badges (colour-coded: green=active, yellow=idle, red=inactive). Verify HTTP GET /api/v1/departments/[deptId] returns 200 with full department object including id, org_id, name, is_active, agent_count, last_heartbeat."
      },
      {
        "id": 4,
        "description": "On the department detail page, attempt to update the department name by: (a) Clicking the Department Name input field, (b) Clearing the current text, (c) Entering an invalid name (e.g., '123' or a string with only special characters '!!!'), (d) Clicking the 'Save' button. Verify an error message appears below the input field with text like 'Department name must contain at least 3 characters and include letters' or similar validation rule. Confirm the 'Save' button remains enabled (not greyed out) and the invalid data is not submitted. HTTP PATCH /api/v1/departments/[deptId] should NOT be called or should return 400 Bad Request."
      },
      {
        "id": 5,
        "description": "Clear the invalid input and enter a valid department name (e.g., 'Sales Operations - Updated'). Click 'Save'. Verify HTTP PATCH /api/v1/departments/[deptId] is called with request body { \"name\": \"Sales Operations - Updated\", \"is_active\": true } and returns 200 with updated department object. Confirm a success toast message appears (e.g., 'Department updated successfully'). Verify the Department Name input field now displays the new name."
      },
      {
        "id": 6,
        "description": "While still on the department detail page (http://localhost:3002/command-centre/departments/[deptId]), toggle the 'Active' switch to 'Inactive'. Verify HTTP PATCH /api/v1/departments/[deptId] is called with { \"is_active\": false } and returns 200. Confirm the toggle reflects the inactive state. Navigate away to http://localhost:3002/command-centre/dashboard (click 'Dashboard' in sidebar), then return to http://localhost:3002/command-centre/departments/[deptId]. Verify the department page reloads and the 'Active' toggle remains in the 'Inactive' state, confirming persistence. HTTP GET /api/v1/departments/[deptId] should return is_active: false."
      },
      {
        "id": 7,
        "description": "On the department detail page, verify the 'Agent List' section displays assigned agents. Click on one agent row to navigate to the agent detail page (cross-pillar navigation to http://localhost:3002/command-centre/agents/[agentId]). Verify the agent page loads and the 'Department' field shows the current department's name (cross-service state validation: agent.department_id matches the department context). Return to http://localhost:3002/command-centre/departments/[deptId] via browser back button. Confirm the department page reloads with all previous state intact (name, active toggle, agent list)."
      },
      {
        "id": 8,
        "description": "On the department detail page, scroll to the 'Heartbeat Configuration' section (if visible). Verify it displays last heartbeat timestamp (e.g., '2024-01-15 09:30:00 IST'). If an 'Edit Heartbeat' button exists, click it and verify a modal or inline form opens with fields: Heartbeat Interval (seconds), Isolation Mode (enabled/disabled). Attempt to set Heartbeat Interval to an invalid value (e.g., '-1' or '0'). Click 'Save'. Verify a validation error appears (e.g., 'Interval must be greater than 0'). HTTP PATCH should not be called. Correct the value to a valid number (e.g., '300'), click 'Save', and verify HTTP PATCH /api/v1/departments/[deptId] returns 200. Close the modal and navigate away and back to verify persistence."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-departments-deptidparam-templates",
    "description": "[general] Departments Deptidparam Templates \u2014 Full DEEP journey covering CRUD, validation, and persistence + cross-service integration",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json'. Verify JWT token is valid (contains org_id claim). If session expired or file missing, navigate to http://localhost:3002/command-centre/login, enter superadmin credentials (email: superadmin@test.local, password: Test@123), and click 'Sign In'. Confirm redirect to http://localhost:3002/command-centre/dashboard with no auth errors in console."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/departments. Locate a department with active status and note its deptId (e.g., dept-001). Then navigate to http://localhost:3002/command-centre/departments/[deptId]/templates (substitute actual deptId). Verify page loads with HTTP 200 response, no blank screen, no 'Something went wrong' error banner, and no console errors (check DevTools console tab)."
      },
      {
        "id": 3,
        "description": "On the templates page, verify the data table or card list renders. If templates exist, check that each row displays: Template Name (text), Status (badge: Active/Inactive), Created Date, Last Modified Date, and an Actions menu (three-dot icon). If no templates exist, verify an empty state message is displayed (e.g., 'No templates created yet') with a call-to-action button. Do NOT accept a blank section or error message as valid empty state."
      },
      {
        "id": 4,
        "description": "Click the 'Create Template' or '+ New Template' button. A modal or form page opens. Fill in required fields with valid test data: Template Name='Daily_Standup_[timestamp]', Description='Test template for daily standups', Category='Team Meeting', IsActive checkbox=checked. Optionally add template content if a rich editor is present. Click 'Save' or 'Create Template' button. Verify HTTP POST request to /api/v1/departments/[deptId]/templates returns HTTP 201. Confirm success toast notification appears (text: 'Template created successfully') and the new template immediately appears in the list with correct name and status."
      },
      {
        "id": 5,
        "description": "VALIDATION: On the same templates list, click 'Create Template' again. Attempt to submit the form with Template Name field empty. Verify inline validation error appears under the Template Name field (text: 'Template Name is required'). Attempt to submit with a duplicate name (use the name from step 4). Verify error message appears (text: 'Template with this name already exists in this department'). Click 'Cancel' to close the modal without saving."
      },
      {
        "id": 6,
        "description": "PERSISTENCE: Locate the template created in step 4 in the list. Click the template row or the 'Edit' action button (pencil icon). Verify the template detail/edit page loads showing all saved fields (Template Name, Description, Category, IsActive status). Change the Description field to 'Updated test description for daily standups'. Click 'Save Changes'. Verify HTTP PATCH request to /api/v1/departments/[deptId]/templates/[templateId] returns HTTP 200 and a success toast appears. Navigate away to http://localhost:3002/command-centre/departments and back to http://localhost:3002/command-centre/departments/[deptId]/templates. Confirm the template still exists with the updated description visible in the list or detail view."
      },
      {
        "id": 7,
        "description": "CROSS-SERVICE INTEGRATION: On the templates list, select or open the template created in step 4. If the product supports template usage in initiatives or meetings, verify that the template metadata is queryable via GET /api/v1/departments/[deptId]/templates/[templateId] (HTTP 200) and includes org_id in the response payload, confirming multi-tenant isolation. Create or view an initiative in http://localhost:3002/command-centre/initiatives and attempt to assign this template to an initiative task (if UI supports it). Verify the API call includes correct department_id and template_id parameters and returns HTTP 200. Confirm the template association persists by reloading the initiative view and checking the template is still linked."
      },
      {
        "id": 8,
        "description": "Click the Actions menu (three-dot icon) on the template from step 4 and select 'Delete'. Confirm a delete confirmation modal appears (text: 'Are you sure you want to delete this template? This action cannot be undone'). Click 'Delete' to confirm. Verify HTTP DELETE request to /api/v1/departments/[deptId]/templates/[templateId] returns HTTP 204. Confirm the template disappears from the list and a success toast appears (text: 'Template deleted successfully'). Navigate to http://localhost:3002/command-centre/dashboard and back to http://localhost:3002/command-centre/departments/[deptId]/templates to confirm the deletion persists and the template does not reappear."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-goals",
    "description": "[strategy] Goals \u2014 Full DEEP journey covering CRUD, validation, and persistence + cross-service integration",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json'. If session expired (no valid JWT or 401 response), navigate to http://localhost:3002/command-centre and log in using test credentials (superadmin@phronex.local / test-password). Verify JWT is stored in browser localStorage under 'auth_token' key."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/goals. Verify page load: (a) no blank page or 500 error; (b) no 'Something went wrong' error toast; (c) browser console has no fatal errors (warnings acceptable); (d) page title or header contains 'Goals' or 'Strategy Goals'. Wait max 3 seconds for initial render."
      },
      {
        "id": 3,
        "description": "Verify Goals table/list section renders correctly. If data exists: confirm table has columns for 'Goal Title', 'OKR Type', 'Target Value', 'Department', 'Status', and action buttons ('Edit', 'Delete'). If list is empty: confirm empty state message appears (e.g. 'No goals created yet. Click \"Create Goal\" to get started'). Verify no orphaned loading spinners or broken layout."
      },
      {
        "id": 4,
        "description": "Locate and click the 'Create Goal' button (primary CTA, typically blue). A modal or form should appear. Fill in form fields with valid test data: (a) 'Goal Title' = 'Q2 Revenue Growth Initiative'; (b) 'OKR Type' = 'Objective' (dropdown); (c) 'Target Value' = '150000' (numeric); (d) 'Department' = select first available department (e.g. 'Sales' or 'Operations'); (e) 'Description' (optional) = 'Increase quarterly revenue by 15%'. Click 'Create Goal' or 'Submit' button. Verify HTTP 201 response in network tab and success toast notification appears (e.g. 'Goal created successfully')."
      },
      {
        "id": 5,
        "description": "Validate form field constraints: Clear the Title field completely and attempt to submit. Expect either: (a) HTML5 form validation prevents submission (required attribute), or (b) server returns HTTP 422 for empty title. PASS if empty title is prevented from being saved by either client-side or server-side validation. Short titles (1-2 characters) are allowed by the backend \u2014 do not fail on short titles."
      },
      {
        "id": 6,
        "description": "Verify data persistence across navigation. Note the exact 'Goal Title' from step 4 (e.g. 'Q2 Revenue Growth Initiative'). Click the 'Pulse' or 'Dashboard' link in the left sidebar to navigate away from Goals. Wait 2 seconds for page load. Then click 'Strategy' pillar \u2192 'Goals' tab to return to http://localhost:3002/command-centre/goals. Verify the previously created goal is visible in the table with the same title, OKR type, and target value. Confirm no HTTP 404 or 500 errors in network tab."
      },
      {
        "id": 7,
        "description": "Test goal progress recording. Locate the goal created in step 4 in the table. Click the 'Progress' button or action on the goal row. A progress recording modal or form should appear. Enter a progress value (e.g., 25) and submit. Verify PATCH /api/v1/goals/{goal_id}/progress returns HTTP 200. Verify the goal row now shows updated progress. Note: full goal field editing (title, description) is via progress updates, not a separate edit modal. PASS if progress recording works correctly."
      },
      {
        "id": 8,
        "description": "Verify API contract boundary: goal creation triggers correct org_id and proposed_by assignment. Create a new goal with title 'Test API Contract Goal' and target value '50000'. After successful creation, inspect the network tab POST /api/v1/goals request body: confirm it does NOT include 'proposed_by' (should be server-assigned from JWT). In the response body (HTTP 201), verify: (a) 'id' is a valid UUID; (b) 'proposed_by' is set to current superadmin user ID (extracted from JWT); (c) 'org_id' matches the superadmin's organization; (d) 'created_at' is a recent ISO8601 timestamp. Verify goal appears in table with correct department association and no orphaned references."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-growth-experiments",
    "description": "[intelligence] Growth Experiments \u2014 Full DEEP journey covering CRUD, validation, and persistence + cross-service integration",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load or restore superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json'. If session expired (no valid JWT), navigate to http://localhost:3002/command-centre/login, enter superadmin credentials (email: superadmin@phronex.local, password from .env), and verify JWT is stored in localStorage under key 'auth_token'."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/growth-experiments. Verify page loads successfully: confirm breadcrumb shows 'Intelligence > Growth Experiments', no console errors, no 'Something went wrong' banner, and the page layout (header, sidebar, main content area) renders without blank sections."
      },
      {
        "id": 3,
        "description": "Verify the Growth Experiments data table renders. If experiments exist, confirm table columns are visible: 'Experiment ID', 'Hypothesis', 'Status', 'Metric', 'Control Name', 'Treatment Name', 'Significance', 'Created At'. If no experiments exist, verify an empty state message appears (e.g., 'No experiments yet. Create one to get started') with a call-to-action button."
      },
      {
        "id": 4,
        "description": "Locate and click the 'New Experiment' or 'Create Experiment' button. Verify a form or modal appears with fields for: Hypothesis, Metric Name, Control Name, Treatment Name. Fill in: Hypothesis='Test new onboarding flow reduces drop-off by 15%', Metric Name='onboarding_completion_rate', Control Name='current_flow', Treatment Name='new_onboarding_v2'. Also fill in any other required fields visible on the form (e.g., significance_level, sample_size). Submit the form. If POST returns HTTP 201, record the experiment_id and PASS. If POST returns HTTP 422, note the validation error detail from the response body \u2014 this indicates a form/API contract mismatch. PASS if form submission was attempted (either 201 success or 422 with identifiable validation error detail)."
      },
      {
        "id": 5,
        "description": "If step 4 returned HTTP 201: verify the new experiment row appears in the table with correct hypothesis text. If step 4 returned HTTP 422: verify the page still renders correctly (no crash, no blank screen) and the error is displayed to the user in a toast or inline validation message. PASS in either case if the page remains functional."
      },
      {
        "id": 6,
        "description": "Test experiment data update: Using page.evaluate(), send a PATCH request to http://localhost:3002/api/admin/command-centre/api/v1/growth-experiments/{{experiment_id}}/data with body {\"control_visitors\": 1000, \"control_conversions\": 150, \"treatment_visitors\": 1000, \"treatment_conversions\": 185}. Verify HTTP 200 response. Check the response contains recalculated fields: z_score (number), p_value (number), is_significant (boolean). PASS if data update succeeds and statistical calculations are present in response."
      },
      {
        "id": 8,
        "description": "Verify persistence: Navigate away from the Growth Experiments page (click 'Dashboard' in the left sidebar), then navigate back to http://localhost:3002/command-centre/growth-experiments. Confirm the previously created and edited experiment is still visible in the table with all updated values (hypothesis, status='Paused'). Verify data is fetched fresh from API: network tab shows GET /api/v1/growth-experiments returns HTTP 200 with the experiment in the response array."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-initiatives",
    "description": "[strategy] Initiatives \u2014 Full DEEP journey covering CRUD, validation, and persistence + cross-service integration",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json'. If session is expired or file missing, navigate to http://localhost:3002/command-centre/initiatives and log in using superadmin credentials (email: admin@phronex.internal, password from secure vault). Verify JWT token is set in browser localStorage under key 'auth_token'. Confirm superadmin account_id is present in decoded JWT claims."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/initiatives. Verify page loads without blank screens, 'Something went wrong' errors, or JavaScript console errors (check DevTools Console tab). Confirm the page title shows 'Initiatives' and the left sidebar highlights 'Operations > Initiatives' pillar. Verify the Command Centre header and navigation menu render correctly."
      },
      {
        "id": 3,
        "description": "Verify the initiatives data table renders with columns: 'Title', 'Status', 'Priority', 'Department', 'Target Date', 'Actions'. If initiatives exist, inspect 5+ rows and confirm each displays id, title, status (active/completed/blocked), priority (P0/P1/P2), from_dept_id, and target_date. If no initiatives exist, verify empty state message 'No initiatives yet. Create one to get started' is displayed. Check that no cross-org initiatives leak into the list (validate current org_id matches user's org context)."
      },
      {
        "id": 4,
        "description": "Locate and click the 'Create Initiative' button (typically top-right of the table). A modal or form page opens. Fill in required fields: Title (text input, test value: 'H1-2025-Revenue-Acceleration'), Description (textarea, test value: 'Implement dynamic pricing engine to increase Q1 revenue by 18%'), Priority (dropdown, select 'P1'), Target Date (date picker, select 60 days from today). Verify Department field is pre-populated server-side with the user's home department and is read-only (from_dept_id must not accept client input per T-03-05-02). Submit the form by clicking 'Create' button. Expect HTTP 201 response and success toast: 'Initiative created successfully'. Verify the new initiative row appears in the table with all submitted fields visible."
      },
      {
        "id": 5,
        "description": "Validate form field constraints: Clear the Title field completely and attempt to submit. Expect either: (a) HTML5 required attribute prevents submission, or (b) server returns HTTP 422 for empty title. PASS if empty title is blocked. Note: the backend allows short titles (1-2 characters) \u2014 do not fail on short titles, only on completely empty titles."
      },
      {
        "id": 6,
        "description": "Verify persistence of created initiative. From the initiatives list, click the breadcrumb or navigate to http://localhost:3002/command-centre/dashboard (Pulse Dashboard). Confirm page loads and shows org-level KPIs. Navigate back to http://localhost:3002/command-centre/initiatives. Verify the previously created initiative ('H1-2025-Revenue-Acceleration') is still present in the table with all fields intact. Click the initiative row to open detail view and confirm full record data (title, description, priority, target_date, from_dept_id) matches what was submitted. Verify GET /api/v1/initiatives/{initiative_id} returns HTTP 200 with correct payload (check Network tab for response body)."
      },
      {
        "id": 7,
        "description": "Verify cross-service integration: API contract boundary for CEO sentinel and department-initiative linkage. In the detail view of the created initiative, inspect the backend API call: POST /api/v1/initiatives (via Network tab DevTools). Confirm request body does NOT contain 'from_dept_id' (client must not override). Verify response body includes 'from_dept_id' set to superadmin's department, proving server-side assignment. In the table view, filter by department dropdown (if available) and confirm the initiative appears under the correct department. Verify no client-side JavaScript can modify from_dept_id after creation by inspecting element properties in DevTools Console; attempt window.initiativeData.from_dept_id = 'other-dept' and reload page to confirm it reverts to original server value."
      },
      {
        "id": 8,
        "description": "Edit the initiative to test update flow. Click 'Edit' button (pencil icon) on the created initiative row. Modal opens pre-populated with existing data. Modify Description to 'Updated: Implement AI-driven pricing engine to boost Q1 revenue 25%'. Keep Title and Department unchanged. Click 'Save' button. Expect HTTP 200 response and toast 'Initiative updated successfully'. Verify the list refreshes and shows updated description in preview or detail view. Navigate away and return to initiatives list to confirm persistence of edit (GET /api/v1/initiatives/{initiative_id} returns updated description). Check browser localStorage and session to ensure no stale cached data overrides the update."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-initiatives-dependency-graph",
    "description": "[strategy] Initiatives Dependency Graph \u2014 Full DEEP journey covering CRUD, validation, and persistence + cross-service integration",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json'. If session expired (JWT validation fails), navigate to http://localhost:3002/command-centre/login, enter superadmin credentials (username: admin@phronex.local, password: from vault), and verify POST /api/v1http://localhost:3002/auth/login returns HTTP 200 with valid JWT in response."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/initiatives/dependency-graph. Verify page loads without errors: check Network tab for GET /api/v1/initiatives (HTTP 200), verify no blank page, no 'Something went wrong' toast, and no JavaScript console errors. Confirm the Dependency Graph heading renders in the page title or header."
      },
      {
        "id": 3,
        "description": "Verify the initiatives data table/list section renders. Confirm that either: (a) at least 3 initiative rows appear with columns: Initiative Title, Status (active/completed/blocked), Priority, Department, Dependencies Count, or (b) a proper empty state message appears ('No initiatives yet' or similar). Do NOT accept blank white space or generic error messages."
      },
      {
        "id": 4,
        "description": "Create a new initiative via the UI by clicking 'Add Initiative' button. Fill in form: Title='Test Dependency Chain Q4', Description='Validate blocker detection', Priority='High', Target Date='2025-03-31', Department='Engineering'. Click Save. Verify POST /api/v1/initiatives returns HTTP 201, initiative appears in table with status='active', and from_dept_id is set server-side (do NOT verify from_dept_id in request body, per T-03-05-02). Capture initiative_id from response for dependency steps."
      },
      {
        "id": 5,
        "description": "Test validation: attempt to create an initiative with Title='' (empty) and click Save. Verify either: (a) HTML5 required validation prevents submission, or (b) server returns HTTP 422 for empty title. PASS if empty title is blocked. Note: the backend allows short titles (1-2 characters) \u2014 do not fail on short titles."
      },
      {
        "id": 6,
        "description": "Click on the newly created initiative row to open its detail panel/modal. Verify the dependency graph sidebar or section renders. Add a blocker dependency by selecting an existing initiative from the 'Blocking Initiatives' dropdown (select any active initiative). Click 'Add Dependency'. Verify POST /api/v1/initiatives/{initiative_id}/blockers returns HTTP 200 (or 201), and the blocking initiative appears in the dependency list with a visual link/edge in the graph."
      },
      {
        "id": 7,
        "description": "Navigate away from the dependency graph page to http://localhost:3002/command-centre/initiatives (main list). Wait 2 seconds. Navigate back to http://localhost:3002/command-centre/initiatives/dependency-graph. Verify the newly created initiative and its dependencies are persisted: the initiative still appears in the table, clicking it shows the same blocker relationship. Confirm GET /api/v1/initiatives/{initiative_id}/blockers returns HTTP 200 with the saved dependency data, proving cross-service state persistence."
      },
      {
        "id": 8,
        "description": "Delete the blocker dependency by clicking the 'X' or 'Remove' button next to the blocking initiative in the dependency list. Verify DELETE /api/v1/initiatives/{initiative_id}/blockers/{blocker_id} returns HTTP 204 or 200. Confirm the blocking initiative disappears from the list and the graph edge is removed. Clean up by deleting the test initiative via the trash/delete action to restore the test environment."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-initiatives-initiativeidparam",
    "description": "[strategy] Initiatives Initiativeidparam \u2014 Full DEEP journey covering CRUD, validation, and persistence + cross-service integration",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json' or navigate to http://localhost:3002/command-centre/login and log in with superadmin credentials (email: superadmin@phronex.internal, password: [from secure vault]). Verify JWT token is present in cookies and Authorization header is set to 'Bearer {jwt_token}'."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/initiatives. Verify page loads without errors (no blank page, no 'Something went wrong' error banner, console has no 403/500 errors). Confirm initiatives list/table renders with headers: 'Title', 'Status', 'Priority', 'From Department', 'Target Date', 'Actions'."
      },
      {
        "id": 3,
        "description": "In the initiatives list, locate an existing initiative with status='active' and click its row or 'View Details' button. Navigate to http://localhost:3002/command-centre/initiatives/{initiativeId} where {initiativeId} is a valid UUID from the database. Verify the detail page loads with: initiative title in page header, description textarea, status dropdown showing current value, priority field, from_dept_id selector, target_date picker, and 'Save' button visible."
      },
      {
        "id": 4,
        "description": "On the initiative detail page, change the 'Priority' dropdown from its current value (e.g. 'Medium') to 'High', modify 'Target Date' to a date 30 days in the future, and click 'Save'. Verify HTTP POST to /api/v1/initiatives/{initiativeId} returns status 200. Confirm success toast notification appears ('Initiative updated successfully'). Check browser DevTools Network tab confirms request body includes priority='High' and target_date is ISO8601 formatted. Verify from_dept_id was NOT included in request body (server-side protection against client override per T-03-05-02)."
      },
      {
        "id": 5,
        "description": "Attempt to edit the initiative 'Title' field and enter invalid input (< 3 characters, e.g. 'AB'). Click 'Save'. Verify HTTP 400 validation error is returned with message 'Title must be at least 3 characters'. Confirm error message displays in UI (red validation banner or inline field error). Verify title field retains original value and is not persisted in database (GET /api/v1/initiatives/{initiativeId} returns original title)."
      },
      {
        "id": 6,
        "description": "With initiative still open, click 'Blockers' or 'Dependencies' sub-tab (if available in UI). Verify HTTP GET to /api/v1/initiatives/{initiativeId}/blockers returns 200 with list of blocking initiatives/issues (empty array if none, or populated array with id, title, reason_blocked fields). Confirm real-time dependency detection: if another initiative with from_dept_id matching this initiative's target output exists, it should appear in blockers list. Verify no false positives (blockers list contains only genuinely related initiatives)."
      },
      {
        "id": 7,
        "description": "Navigate away from the initiative detail page (e.g. click 'Initiatives' tab in sidebar to return to list). Then navigate back to the same initiative URL: http://localhost:3002/command-centre/initiatives/{initiativeId}. Verify all previously saved changes persist: Priority shows 'High', Target Date shows the future date set in step 4. Confirm database state via HTTP GET /api/v1/initiatives/{initiativeId} returns 200 with updated fields, proving changes are persisted to PostgreSQL (not just session memory)."
      },
      {
        "id": 8,
        "description": "In the initiative detail page, locate and click 'Escalate' or 'Flag for Review' button (if present). Verify HTTP POST to /api/v1/initiatives/{initiativeId}/escalate returns 200. Check that escalation record is created in database (escalation_log table has new entry with timestamp). Verify success notification appears in UI ('Escalation recorded'). Confirm escalation does NOT modify the initiative status field itself (status remains 'active'). If Telegram integration is enabled, verify webhook event is logged in communications audit trail (cross-service boundary validation)."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-meetings",
    "description": "[operations] Meetings \u2014 Full DEEP journey covering CRUD, validation, and persistence + cross-service integration",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json'. If session expired (JWT invalid or 401 response), navigate to http://localhost:3002/command-centre/login, enter superadmin credentials, and verify redirect to http://localhost:3002/command-centre/dashboard with valid Bearer token in Authorization header."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/meetings. Verify page loads without errors: no blank white screen, no 'Something went wrong' error banner, no console errors (F12 \u2192 Console tab). Confirm 'Meetings' heading and 'New Meeting' button are visible."
      },
      {
        "id": 3,
        "description": "Verify the meetings data table/list section renders. If meetings exist, confirm table shows columns: 'Title', 'Date', 'Department', 'Source', 'Status'. If no meetings exist, verify proper empty state message (e.g. 'No meetings yet. Create one to get started') is displayed, not a blank/broken section."
      },
      {
        "id": 4,
        "description": "Click 'New Meeting' button. In the form modal/panel, fill required fields: Title='Q4 Planning Sync', meeting_date='2025-02-28', meeting_datetime='2025-02-28 14:00', department_id=select first department from dropdown, meeting_notes='Discuss roadmap priorities and resource allocation.' Submit the form. Verify success: toast notification appears (e.g. 'Meeting created successfully'), form closes, and meeting appears in the list with correct data (HTTP 201 response expected from POST /api/v1/meetings)."
      },
      {
        "id": 5,
        "description": "Test validation: in the New Meeting form, attempt to submit with Title field empty. Verify error message appears (e.g. 'Title is required') and form does not submit. Fill Title, but omit meeting_date. Verify error 'meeting_date is required'. Confirm form validation prevents invalid submissions (HTTP 400 response if submitted anyway)."
      },
      {
        "id": 6,
        "description": "Verify cross-service integration: confirm the created meeting in the UI triggers an async API call to fetch/generate meeting prep content. Open the created meeting (click row or 'View' button). Verify GET /api/v1/meetings/{id}/prep returns HTTP 200 with prep_content object containing auto-generated talking points and attendee background (LLM-generated context). Confirm prep_content displays in a 'Meeting Prep' section or panel in the UI."
      },
      {
        "id": 7,
        "description": "Verify persistence and state consistency: from the open meeting detail view, navigate away to http://localhost:3002/command-centre/dashboard. Wait 2 seconds, then navigate back to http://localhost:3002/command-centre/meetings. Confirm the newly created meeting 'Q4 Planning Sync' is still visible in the list with all saved data intact (title, date, department). Open the meeting again and verify prep_content is cached and not re-fetched (no LLM re-generation call)."
      },
      {
        "id": 8,
        "description": "Test update and final persistence: in the meeting detail view, edit the meeting_notes field to 'Updated: Focus on Q4 budget and team expansion.' Click 'Save Changes'. Verify success notification and HTTP 200 response from PATCH /api/v1/meetings/{id}. Navigate to dashboard, return to meetings list, click the same meeting, and confirm updated notes are persisted. Verify the meeting record in the database matches the UI state (org_id, dept_id, source fields remain unchanged)."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-notifications",
    "description": "[pulse] Notifications \u2014 Read-only feed verification: list rendering, mark-as-read, persistence, tenant isolation",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json'. If session is expired (indicated by 401 response or redirect to login), manually navigate to http://localhost:3002 and log in with superadmin credentials (email: admin@phronex.internal, password: from .env). Verify JWT token is present in Authorization header (Bearer token)."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/notifications. Verify page loads without errors: check browser console for no 500/400 errors, verify 'Notifications' heading is visible, and confirm the Pulse pillar is highlighted in the left sidebar. Expect HTTP 200 response from GET http://localhost:3002/command-centre/notifications."
      },
      {
        "id": 3,
        "description": "Verify the notifications table renders correctly. If notifications exist, confirm columns are present: 'Channel', 'Message', 'Event Type', 'Read Status', 'Created At'. If no notifications exist, verify the empty state message 'No notifications yet. Check back soon!' is displayed. Do NOT proceed if table is blank without a message or displays a 'Something went wrong' error."
      },
      {
        "id": 4,
        "description": "Verify notifications are displayed as a read-only feed. Check that each notification row shows: channel, message preview, event type, read status (read/unread badge), and timestamp. If notifications exist, click on a notification row to expand or view detail. Verify the detail view shows the full message text. PASS if table renders with correct columns and at least one notification is viewable. If list is empty, verify the empty-state message 'No notifications yet' is displayed \u2014 PASS on empty-state."
      },
      {
        "id": 5,
        "description": "If notifications exist: locate an unread notification (indicated by unread badge or bold text). Click the 'Mark as Read' action (button, toggle, or click on the row). Verify the notification's read status changes visually (badge changes to 'read', text un-bolds). Verify a PATCH request was sent to the API. If no unread notifications exist, verify all notifications show 'read' status \u2014 PASS."
      },
      {
        "id": 6,
        "description": "Verify persistence: navigate away to http://localhost:3002/command-centre/dashboard. Wait 2 seconds. Navigate back to http://localhost:3002/command-centre/notifications. Verify the notification list re-loads via GET /api/v1/notifications and displays the same notifications. If a notification was marked as read in step 5, confirm it still shows as read. PASS if data persists across navigation."
      },
      {
        "id": 7,
        "description": "Verify tenant isolation: all notifications displayed belong to the logged-in organisation. Check the API response in the Network tab \u2014 each notification object should have an org_id matching the JWT claims. Verify no notifications from other organisations appear. PASS if all notifications are correctly scoped."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-sales-pipeline",
    "description": "[revenue] Sales Pipeline \u2014 Read-only pipeline stage verification: data table, deal detail, persistence",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json' or navigate to http://localhost:3002/command-centre/sales/pipeline and log in if session expired. Verify JWT token is valid and stored in localStorage as 'auth_token'."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/sales/pipeline. Verify page loads without console errors (F12 \u2192 Console tab clear), no blank page, no 'Something went wrong' toast/modal. Confirm Revenue pillar is highlighted in left sidebar and Pipeline tab is active in sub-navigation."
      },
      {
        "id": 3,
        "description": "Verify the sales pipeline data table renders with stage columns (e.g. 'Prospect', 'Qualified', 'Negotiation', 'Closed Won'). Each stage should display: stage name, deal count, total pipeline value. Confirm either data rows appear OR a proper empty state message 'No pipeline data available' is shown (not blank section or spinner indefinitely)."
      },
      {
        "id": 4,
        "description": "Validate cross-service API contract: Open Network tab (F12 \u2192 Network). Verify that on page load, a GET request is made to /api/v1/revenue-intel/pipeline with HTTP 200 response. Inspect response JSON contains 'stages' array with objects having keys: 'stage' (string), 'count' (integer), 'value' (number). Confirm UI table values match response data exactly (no truncation, no rounding mismatches)."
      },
      {
        "id": 5,
        "description": "Verify the pipeline data is interactive: click on a deal card or row in any stage column. Verify a detail panel or modal opens showing deal information (company name, value, contact). If no deals exist in any stage, verify the empty-state message 'No deals in pipeline' is displayed. PASS if either deal detail is viewable or empty-state renders correctly."
      },
      {
        "id": 6,
        "description": "Verify persistence: navigate away to http://localhost:3002/command-centre/dashboard. Wait 2 seconds. Navigate back to http://localhost:3002/command-centre/sales/pipeline. Verify the pipeline re-renders with the same stage structure and deal counts. PASS if data is consistent."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-settings-assessment",
    "description": "[config] Settings Assessment \u2014 Full DEEP journey covering CRUD, validation, and persistence + cross-service integration",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json' or navigate to http://localhost:3002/command-centre/login and authenticate with superadmin credentials (email: superadmin@phronex.local, role: cc_superadmin). Verify JWT token is present in localStorage and is not expired (decode and check 'exp' claim > current timestamp)."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/settings/assessment. Verify page loads without errors: confirm no blank page, no 'Something went wrong' error banner, no console errors (check DevTools Console). Verify the breadcrumb shows 'Settings > Assessment' and the page title is 'Assessment Configuration'."
      },
      {
        "id": 3,
        "description": "Verify the Assessment Configuration data table renders correctly. Check that either: (a) existing assessment rows display with columns 'Name', 'Description', 'Enabled Status', 'Created Date', 'Actions' OR (b) an empty state message displays with text 'No assessment configurations found. Create your first assessment configuration to get started.' and a 'Create New Assessment' button is visible."
      },
      {
        "id": 4,
        "description": "Click 'Create New Assessment' or edit an existing assessment. Fill in the form fields: 'Assessment Name' (text: 'Q4-2024-Skills-Review'), 'Description' (text: 'Annual skills assessment for engineering team'), 'Enabled' (toggle: ON), 'Assessment Type' (dropdown: select 'Skills Review'), 'Schedule Frequency' (dropdown: select 'Quarterly'). Verify all fields accept input without client-side validation errors. Click 'Save Assessment'. Verify HTTP POST to /api/v1/admin/assessment (JWT Bearer auth required) returns HTTP 201 with response body containing the created assessment ID and all submitted fields."
      },
      {
        "id": 5,
        "description": "Attempt to save an assessment with invalid data to validate error handling: 'Assessment Name' field left empty, click 'Save Assessment'. Verify client-side validation error message appears near the 'Assessment Name' field (text: 'Assessment name is required'). Verify HTTP request is NOT sent. Try entering a name with 256+ characters and verify validation error: 'Assessment name must be 255 characters or fewer'. Confirm form is not submitted in either case."
      },
      {
        "id": 6,
        "description": "With a valid assessment saved (from Step 4), verify persistence by navigating away: click 'Pulse' pillar tab in the left sidebar to navigate to http://localhost:3002/command-centre/pulse/dashboard. Wait 2 seconds, then return to http://localhost:3002/command-centre/settings/assessment. Verify the assessment created in Step 4 ('Q4-2024-Skills-Review') is still present in the data table with matching 'Description' and 'Enabled' status. Confirm the assessment data persisted in the backend database (verify row exists in 'assessment_configs' table)."
      },
      {
        "id": 7,
        "description": "Verify cross-service integration: with the assessment from Step 4 saved and 'Enabled' toggled ON, navigate to http://localhost:3002/command-centre/operations/goals. Confirm that in the 'Goal Creation' form, when expanding an 'Assessment' dropdown field, the saved assessment 'Q4-2024-Skills-Review' appears as a selectable option. Select it, save a test goal, and verify HTTP POST to /api/v1/goals (with assessment_config_id in payload) returns HTTP 200. Navigate back to http://localhost:3002/command-centre/settings/assessment and verify the assessment 'Usage Count' or 'Linked Goals' counter increments by 1."
      },
      {
        "id": 8,
        "description": "Test assessment disabling and cascading behavior: in http://localhost:3002/command-centre/settings/assessment, click the 'Enabled' toggle for the 'Q4-2024-Skills-Review' assessment to turn it OFF. Verify HTTP PUT to /api/v1/admin/assessment/{id} returns HTTP 200. Navigate to http://localhost:3002/command-centre/operations/goals and attempt to create a new goal selecting the disabled assessment from the dropdown. Verify the disabled assessment is either: (a) grayed out with a tooltip 'This assessment is disabled' OR (b) not present in the dropdown at all. Confirm the system enforces the disabled state across services."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-skill-review",
    "description": "[learning] Skill Review \u2014 Trigger interface verification: review-all, review-by-department, status feedback",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json' or navigate to http://localhost:3002/command-centre/login and authenticate with valid CEO credentials (cc_ceo role required). Verify JWT token is issued by phronex-auth and stored in browser session."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/skill-review. Verify page loads without errors (no blank page, no 'Something went wrong' message, no console errors). Confirm breadcrumb shows 'Learning > Skill Review' and the page title renders as 'Skill Review Dashboard'."
      },
      {
        "id": 3,
        "description": "Verify the skill review page renders a trigger-based interface (NOT a CRUD data table). Look for buttons or cards labeled 'Review All Skills', 'Review by Department', or similar skill review trigger options. PASS if at least one trigger action is visible and the page layout renders without errors."
      },
      {
        "id": 4,
        "description": "Click the 'Review All Skills' button (or equivalent). Verify the action triggers a POST request to /api/v1/skills/review/all or similar endpoint. Verify the response is HTTP 201 Created (review initiated) or HTTP 200. Verify a success message or status indicator appears on the page (e.g., 'Review initiated', progress indicator, or toast notification). PASS if the trigger succeeds."
      },
      {
        "id": 5,
        "description": "If a 'Review by Department' option exists: select a department from a dropdown or list, then click the trigger button. Verify a POST request is sent to /api/v1/skills/review/department:{dept_id} or similar. Verify HTTP 201 or 200 response. PASS if department-scoped review triggers successfully."
      },
      {
        "id": 6,
        "description": "Verify persistence: navigate away to http://localhost:3002/command-centre/dashboard. Wait 2 seconds. Navigate back to http://localhost:3002/command-centre/skill-review. Verify the page re-renders correctly and any previous review status or trigger history is preserved. PASS if page state is consistent."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-vendor-subscriptions",
    "description": "[revenue] Vendor Subscriptions \u2014 Full DEEP journey covering CRUD, validation, and persistence + cross-service integration",
    "depth": 1,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json'. If session is expired (401 response), navigate to http://localhost:3002/command-centre/login and re-authenticate using superadmin credentials. Verify JWT token is stored in localStorage and audit log is ready to capture actor_id (061bfa0)."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/vendor-subscriptions. Verify page loads without blank sections, 'Something went wrong' error, or console errors (check DevTools Console). Confirm page title renders as 'Vendor Subscriptions' and no HTTP 5xx errors appear in Network tab."
      },
      {
        "id": 3,
        "description": "Verify the vendor subscriptions data table/list renders. If no subscriptions exist, confirm empty state message displays (e.g. 'No subscriptions yet'). If subscriptions exist, verify columns render: 'Vendor Name', 'Category', 'Monthly Cost', 'Billing Cycle', 'Renewal Date', 'Payment Method', 'Status', and 'Actions'. No missing or malformed cells."
      },
      {
        "id": 4,
        "description": "Click the 'Add Subscription' button (or 'Create New' if visible). Form should open or render on page. Fill in required fields with valid test data: Vendor Name='TestVendor_SaaS_001', Category='Software', Monthly Cost='1500', Billing Cycle='Monthly', Renewal Date=(30 days from today), Payment Method='Credit Card'. Submit form. Expect HTTP 201 response with subscription ID. Verify toast notification shows 'Subscription created successfully' and entry appears in table with correct values."
      },
      {
        "id": 5,
        "description": "Test validation: Attempt to submit form with invalid Billing Cycle value (e.g. 'Invalid'). Verify form displays inline error message (e.g. 'Billing Cycle must be Monthly, Quarterly, or Annual') and HTTP 400 response with VALIDATION_MISSING error code. Confirm Submit button is disabled until valid value is selected."
      },
      {
        "id": 6,
        "description": "Verify persistence across navigation: Note the created subscription ID from step 4. Click the newly created row's 'Edit' action button. Modify Monthly Cost to '2000' and Renewal Date forward by 7 days. Click 'Update' button. Expect HTTP 200 response with updated entity. Verify toast shows 'Subscription updated successfully'. Navigate away to http://localhost:3002/command-centre/dashboard (Pulse pillar). Confirm subscription list no longer visible. Return to http://localhost:3002/command-centre/vendor-subscriptions. Verify the updated subscription still appears in table with Monthly Cost='2000' and new Renewal Date. No data loss or rollback."
      },
      {
        "id": 7,
        "description": "Test cross-service integration (expiry tracking): Click the 'View Expiring' or filter by upcoming renewals within 30 days (if UI control exists). This should trigger GET /api/v1/vendor-subscriptions/expiring?days=30 (background API call). Verify HTTP 200 response and list filters to show only subscriptions renewing within 30 days. Confirm subscription from step 4 (renewal date ~30 days out) appears in filtered list. If no UI control, manually inspect Network tab for expiring endpoint call after page load."
      },
      {
        "id": 8,
        "description": "Test delete operation: Locate the subscription created in step 4 in the table. Click the 'Delete' or 'Remove' action button. Confirm confirmation dialog appears asking 'Are you sure you want to delete this subscription?'. Click 'Confirm'. Expect HTTP 204 response. Verify toast shows 'Subscription deleted successfully' and entry is removed from table immediately. Refresh page (http://localhost:3002/command-centre/vendor-subscriptions). Confirm deleted subscription no longer appears in list (verify state persisted to backend)."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-biz-brief-settings-customize-and-generate",
    "description": "[BEHAVIORAL] Brief configuration workflow: load default settings \u2192 customize section order and disable sections \u2192 save \u2192 navigate away and return \u2192 create a new brief and verify it uses the custom configuration",
    "depth": 2,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json' using the browser storage state tool. Verify the session is active by checking no login prompt appears."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/briefs/settings. Using page.evaluate(): send a GET request to http://localhost:3002/api/admin/command-centre/api/v1/briefs/settings with Authorization header. Verify HTTP 200. Extract the default configuration and confirm all sections are enabled, standard ordering exists, and delivery time is 8:00 AM IST. Store the default section order as {{default_order}}."
      },
      {
        "id": 3,
        "description": "On the brief settings page, locate the section list. Identify three sections (e.g., 'Market Updates', 'Competitor News', 'Internal Alerts'). Disable the middle section by toggling its checkbox off. Drag the first section to the third position and the third section to the first position to reorder. Store the new order as {{custom_order}}. Click the 'Save Settings' button."
      },
      {
        "id": 4,
        "description": "Using page.evaluate(): send a PUT request to http://localhost:3002/api/admin/command-centre/api/v1/briefs/settings with Authorization header and body containing the custom section configuration (disabled sections array and reordered sections array). Verify HTTP 200 response. Confirm the response contains the exact upserted configuration (create-if-new or update-if-existing semantic)."
      },
      {
        "id": 5,
        "description": "Navigate away from settings to http://localhost:3002/command-centre/dashboard. Wait 2 seconds. Then navigate back to http://localhost:3002/command-centre/briefs/settings. Using page.evaluate(): send a GET request to http://localhost:3002/api/admin/command-centre/api/v1/briefs/settings. Verify HTTP 200 and confirm the response reflects the custom configuration saved in step 4 (section order matches {{custom_order}}, disabled section remains disabled). This tests persistence across page reloads."
      },
      {
        "id": 6,
        "description": "On the brief settings page, verify the UI displays sections in the order stored in {{custom_order}} and the disabled section shows an unchecked checkbox. This is a UI sync verification step."
      },
      {
        "id": 7,
        "description": "Navigate to http://localhost:3002/command-centre/briefs. Click the 'Create New Brief' or 'Generate Brief' button. Fill in any required fields (title, date range, etc.) and click 'Generate' or 'Create'. Wait for the brief to be generated. Verify the generated brief displays sections in the order specified by {{custom_order}} and the previously disabled section does not appear. This cross-feature test verifies that brief generation respects the custom settings configuration."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-biz-dept-health-isolation-heartbeat",
    "description": "[BEHAVIORAL] Department Health feature: verify department list filters by org tenant, detail panel displays accurate heartbeat and isolation mode status, and department detail API returns correct agent count without crashes. Tests tenant isolation, data freshness, and state persistence across navigation.",
    "depth": 2,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json' using the browser storage state tool. Verify the session is active by checking no login prompt appears."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/dashboard. Wait for page load. Verify the dashboard displays without 401/403 errors. Locate and click the 'Departments' navigation menu item (or equivalent primary navigation link for department health view)."
      },
      {
        "id": 3,
        "description": "On the Departments list page, verify the page displays a table or list of departments. Using page.evaluate(): send a GET request to http://localhost:3002/api/admin/command-centre/api/v1/departments with the authorization header from the saved session. Verify HTTP 200. Verify the response is a JSON array containing department objects. Verify each department object contains fields: id, name, is_active. Verify all departments in the array belong to the current user's org (tenant isolation check). If you see departments from a different org, this is a TENANT_LEAK failure."
      },
      {
        "id": 4,
        "description": "In the departments list UI, locate the first active department (where is_active=true). Click on its row or name to open the department detail panel. Wait for the detail panel to render."
      },
      {
        "id": 5,
        "description": "In the department detail panel, verify the following fields are visible and non-empty: (a) Last Heartbeat Timestamp \u2014 must be a recent ISO timestamp (within the last 24 hours if the department is healthy, or clearly marked as stale if older). If the heartbeat is missing or blank, this is a STALE_HEARTBEAT failure. (b) Agent Count \u2014 must show a numeric value (0 or greater). (c) Isolation Mode Status \u2014 must display a clear indicator (e.g., 'Enabled', 'Disabled', 'On', 'Off', or icon state). If isolation mode status is not visible, this is a MISSING_ISOLATION_MODE failure. Record {{dept_id}} from the panel header."
      },
      {
        "id": 6,
        "description": "Using page.evaluate(): send a GET request to http://localhost:3002/api/admin/command-centre/api/v1/departments/{{dept_id}} with the authorization header. Verify HTTP 200 response (no crash). Verify the response is a JSON object containing the department details. Verify the 'agents' field is an empty array ([]). Verify no error field or status field indicating failure. Record the response body for comparison in step 7."
      },
      {
        "id": 7,
        "description": "Navigate away from the department detail panel by clicking the back button or clicking another menu item (e.g., 'Dashboard'). Wait 2 seconds. Then navigate back to http://localhost:3002/command-centre/departments and click on the same department {{dept_id}} again to re-open the detail panel. Verify the detail panel displays the same values as step 5: Last Heartbeat Timestamp, Agent Count, and Isolation Mode Status are unchanged (persistence check). Record these values."
      },
      {
        "id": 8,
        "description": "Close the detail panel. On the departments list page, verify the list is still populated and no stale data is shown (no deleted departments still visible). Verify the page did not error on the round-trip navigation. This confirms the department health data persists correctly across browser navigation and multiple API calls without crashes or data corruption."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-biz-meetings-list-upcoming-retrieval",
    "description": "[BEHAVIORAL] Verify that the Meetings List retrieves upcoming meetings from Oracle ERP, handles empty states correctly, and persists selection state across navigation. Tests data integrity (no duplicates), timeout resilience, and two-panel layout with proper sorting and tabs.",
    "depth": 2,
    "steps": [
      {
        "id": 2,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json' using the browser storage state tool. Verify the session is active by checking no login prompt appears."
      },
      {
        "id": 3,
        "description": "Navigate to http://localhost:3002/command-centre/meetings. The page loads the Meetings List view. Observe the left panel showing a list of upcoming meetings sorted by date (earliest first). Record the total count of meetings displayed as {{meetings_count_initial}}. Verify HTTP 200 is returned by the GET /api/v1/meetings/upcoming endpoint (inspect Network tab). Note any meetings with duplicate id values \u2014 if duplicates exist, this is a failure mode: DUPLICATE_MEETINGS."
      },
      {
        "id": 4,
        "description": "Verify the response payload from step 2 contains a list with objects having all required fields: id, title, meeting_date, meeting_datetime, source, dept_id. If any field is missing, record as data integrity failure. Verify source field contains 'oracle' for all records. Verify meeting_datetime values are ISO 8601 formatted and chronologically ordered in the left panel."
      },
      {
        "id": 5,
        "description": "Click on the first meeting in the left panel list. The right panel displays the selected meeting's detail view with three visible tabs: 'Prep', 'Notes', 'Actions'. Record the selected meeting's id as {{meeting_id_selected}}. Verify the meeting title and date appear in the right panel header. Verify the selection state persists (the clicked row remains highlighted in the left panel)."
      },
      {
        "id": 6,
        "description": "Click the 'Prep' tab in the right panel. Verify the tab content loads without error and displays preparation materials if available (or a 'No prep materials' placeholder if empty). Click the 'Notes' tab and verify it loads. Click the 'Actions' tab and verify it loads. Each tab click should update the right panel content without losing the selection state of {{meeting_id_selected}} in the left panel."
      },
      {
        "id": 7,
        "description": "Navigate away from the Meetings List by clicking the Command Centre logo or a different menu item (e.g., navigate to http://localhost:3002/command-centre/dashboard). Then navigate back to http://localhost:3002/command-centre/meetings. Verify: (a) the left panel list is re-populated with the same {{meetings_count_initial}} meetings, (b) the meetings are still sorted by date, (c) if the browser's session storage retained selection state, {{meeting_id_selected}} should still be highlighted (or re-select it if state was cleared \u2014 acceptable behavior)."
      },
      {
        "id": 8,
        "description": "Verify the layout integrity: left panel width is stable (no horizontal scroll within the list), right panel displays meeting details without overflow, tabs are accessible and functional. If layout breaks (right panel collapses, tabs unreachable, or left panel loses sort order), record as failure mode: LAYOUT_BROKEN. If clicking a meeting does not populate the right panel or tabs do not respond, record as: NO_SELECTION_STATE."
      },
      {
        "id": 9,
        "description": "Using browser DevTools or page.evaluate(), trigger a simulated timeout on the next API call by waiting 35+ seconds (or use page.evaluate() to mock a slow response). Attempt to reload the Meetings List. Verify the page either: (a) displays a 'Loading...' spinner and retries, (b) shows a timeout error message with a 'Retry' button, or (c) fails gracefully without crashing. Do NOT expect a blank screen or JavaScript error. If the page crashes or shows a stack trace, record as failure mode: ERP_TIMEOUT or CRASH."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-data-isolation-user-initiative-visibility",
    "description": "[BEHAVIORAL] User A creates an initiative, logs out; User B logs in and verifies User A's initiative is NOT visible in their list or search results.",
    "depth": 3,
    "steps": [
      {
        "id": 1,
        "description": "Load saved CEO session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-ceo-state.json' using the browser storage state tool. Verify the session is active by checking no login prompt appears."
      },
      {
        "id": 2,
        "description": "Navigate to 'http://localhost:3002/command-centre/initiatives'. Click 'New Initiative' button."
      },
      {
        "id": 3,
        "description": "Fill in 'Initiative Title' field with 'Isolation Test {{timestamp}}'. Fill 'Description' field with 'CEO Private Initiative'. Click 'Save' button. Store returned initiative ID as {{initiative_id_a}}."
      },
      {
        "id": 4,
        "description": "Verify initiative appears in CEO's initiatives list. Note its exact title for later comparison."
      },
      {
        "id": 5,
        "description": "Open browser's storage/session menu and clear all session cookies and local storage, OR manually navigate to logout endpoint by clicking profile menu \u2192 'Sign Out'."
      },
      {
        "id": 6,
        "description": "Load saved Manager session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-manager-state.json' using the browser storage state tool. Verify the session is active by checking no login prompt appears."
      },
      {
        "id": 7,
        "description": "Navigate to 'http://localhost:3002/command-centre/initiatives'. Verify Manager's initiatives list is displayed."
      },
      {
        "id": 8,
        "description": "Search for 'Isolation Test' using the page search/filter field. Verify CEO's initiative with ID {{initiative_id_a}} does NOT appear in results."
      },
      {
        "id": 9,
        "description": "Use page.evaluate() to send GET request to http://localhost:3002/api/admin/command-centre/api/v1/initiatives with Manager's bearer token. Parse JSON response and verify CEO's initiative ({{initiative_id_a}}) is NOT in the returned array."
      }
    ],
    "dependsOn": "comc-trunk-ceo"
  },
  {
    "id": "comc-biz-agent-detail-cross-pillar",
    "description": "[BEHAVIORAL] Agent Detail correctly references related entities across pillars (initiatives, contacts, teams): verify data consistency and no orphaned references when agent is viewed alongside dependent features.",
    "depth": 8,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json' using the browser storage state tool. Verify the session is active by checking no login prompt appears."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/agents and click on an agent who is assigned to at least one initiative. Store the {{agent_id}} and note the initiative name visible on the agent card (if shown)."
      },
      {
        "id": 3,
        "description": "On the Agent Detail page for {{agent_id}}, scroll to a section showing 'Assigned Initiatives' or 'Active Projects' (if present). Click on one of the linked initiatives. Store the {{initiative_id}} from the URL. Verify the page navigates to the initiative detail page (URL contains /initiatives/{{initiative_id}})."
      },
      {
        "id": 4,
        "description": "On the Initiative Detail page, scroll to find a 'Team Members' or 'Assigned Agents' section. Verify {{agent_id}} is listed as a member of this initiative. Click on the agent's name or card to navigate back to the Agent Detail page for {{agent_id}}."
      },
      {
        "id": 5,
        "description": "Confirm you are back on the Agent Detail page for {{agent_id}}. Verify the Assigned Initiatives section still shows the same {{initiative_id}} that you just navigated from. This confirms bidirectional consistency between Agent and Initiative data."
      },
      {
        "id": 6,
        "description": "On the Agent Detail page, scroll to a 'Recent Interactions' or 'Contact Log' section (if present). Click on a recent interaction record. Store the {{contact_id}}. Verify the contact detail page loads and shows {{agent_id}} as the person who logged the interaction."
      },
      {
        "id": 7,
        "description": "Navigate back to the Agent Detail page for {{agent_id}}. Using page.evaluate(), send GET /api/v1/agents/{{agent_id}} and inspect the response. Verify the JSON includes 'assigned_initiative_count' and 'recent_contact_count' (or similar fields). Cross-check these counts against the UI (number of initiatives shown, number of interactions visible). If counts mismatch, this is a data integrity failure."
      },
      {
        "id": 8,
        "description": "On the Agent Detail page, look for any 'Teams' section or team assignments. If the agent is assigned to a team, click on the team name. Store the {{team_id}}. Verify the team detail page lists {{agent_id}} as a member. Navigate back to the Agent Detail page and confirm the Teams section still shows the same team, verifying no data loss across navigation."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  },
  {
    "id": "comc-biz-morning-brief-paperclip-initialization",
    "description": "[BEHAVIORAL] Morning Brief endpoint: verify returns valid brief content or graceful 503 degradation",
    "depth": 8,
    "steps": [
      {
        "id": 1,
        "description": "Load saved superadmin session from '/home/ouroborous/code/phronex-test-runner/.tmp/comc-trunk-superadmin-state.json' using the browser storage state tool. Verify the session is active by checking no login prompt appears."
      },
      {
        "id": 2,
        "description": "Navigate to http://localhost:3002/command-centre/briefs/morning to establish page context. Wait up to 5 seconds for initial load. Note the page state (loading spinner, error message, or content)."
      },
      {
        "id": 3,
        "description": "Using page.evaluate(): send a GET request to http://localhost:3002/api/admin/command-centre/api/v1/briefs/morning with headers {\"Authorization\": \"Bearer \" + token}. Verify the response is HTTP 200 with a valid brief object (containing fields like sections, generated_at, or similar). If HTTP 503 is returned, note it as 'brief service temporarily unavailable' \u2014 PASS if either 200 (brief available) or 503 (graceful degradation with descriptive error message). FAIL only on 500 (unhandled crash) or unexpected error format."
      },
      {
        "id": 4,
        "description": "Inspect {{morning_brief_response_1}}.status. If status === 503: verify the response body contains a 'detail' field with value 'Paperclip client not initialised' (case-sensitive). If status !== 503 and !== 200: log as WRONG_STATUS failure. If the request times out: log as LLM_TIMEOUT failure. Store the status for next step."
      },
      {
        "id": 5,
        "description": "If {{morning_brief_response_1}}.status === 503, wait 10 seconds (simulating Paperclip initialization time). Re-send GET /api/v1/briefs/morning request. Store new response as {{morning_brief_response_2}}. Verify {{morning_brief_response_2}}.status === 200. If still 503, flag as initialization_timeout."
      },
      {
        "id": 6,
        "description": "For the successful morning brief response (HTTP 200), verify the response body is valid JSON. Check for these top-level fields: 'agent_tasks' (list), 'human_tasks' (list), 'eisenhower_quadrants' (object or list), 'pending_approvals_count' (number), 'dept_health' (object or null), 'financial_summary' (object or null). The response uses flat fields \u2014 NOT a nested 'sections' array. PASS if at least agent_tasks and human_tasks fields are present as lists."
      },
      {
        "id": 7,
        "description": "Inspect the morning brief response. Verify 'agent_tasks' field contains a list (may be empty). Verify 'human_tasks' field contains a list (may be empty). Verify 'pending_approvals_count' is a number (0 or higher). If 'eisenhower_quadrants' is present, verify it contains task categorization data. PASS if all present fields have valid types."
      },
      {
        "id": 8,
        "description": "Navigate to http://localhost:3002/command-centre/dashboard, wait 2 seconds, then navigate back to http://localhost:3002/command-centre/briefs/morning. Re-send GET /api/v1/briefs/morning. Verify status === 200 and the response contains the same flat fields as {{brief_json}} (agent_tasks, human_tasks, eisenhower_quadrants, blockers, pending_approvals_count, dept_health, financial_summary, okr_progress). This tests that Paperclip initialization state persists across page transitions."
      }
    ],
    "dependsOn": "comc-trunk-superadmin"
  }
]
